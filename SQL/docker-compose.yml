services:
  postgres:
    image: postgres:15
    container_name: postgres_db
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: demo
    ports:
      - "5433:5432"
    volumes:
      # 👇 改成你的家目录路径（使用绝对路径！）
      - /home/matchaeggtart/Workspace/SQL/postgres-data:/var/lib/postgresql/data
